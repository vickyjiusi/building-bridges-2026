"use client";import{FormEvent,useState}from"react";import{createClient}from"@supabase/supabase-js";import type{Submission}from"@/lib/submissions";
export default function Dashboard(){const[rows,setRows]=useState<Submission[]>([]),[token,setToken]=useState(""),[error,setError]=useState("");async function login(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),{data,error}=await createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!).auth.signInWithPassword({email:String(f.get("email")),password:String(f.get("password"))});if(error||!data.session){setError("邮箱或密码不正确");return}const t=data.session.access_token,r=await fetch("/api/admin/submissions",{headers:{authorization:`Bearer ${t}`}});if(!r.ok){setError("该账号没有教师权限");return}setToken(t);setRows(await r.json())}async function action(x:Submission,status:string){const r=await fetch("/api/admin/submissions",{method:"PATCH",headers:{"content-type":"application/json",authorization:`Bearer ${token}`},body:JSON.stringify({...x,status})});if(r.ok)setRows(a=>a.map(v=>v.id===x.id?{...x,status:status==="approved"?"published":status}:v))}if(!token)return <form className="form-shell" onSubmit={login}><h2>教师登录</h2><label>邮箱<input name="email" type="email" required/></label><label>密码<input name="password" type="password" required/></label>{error&&<p className="error">{error}</p>}<button className="submit">进入审核台 →</button></form>;return <section className="dashboard">{rows.map(x=><article className="review" key={x.id}><div className="meta"><b>{x.status}</b><span>GROUP {x.groupNumber} · {x.contentType}</span></div><div className="review-main">{x.imageUrl&&<img src={x.imageUrl} alt="提交预览"/>}<div><h2>{x.title}</h2><p>{x.body}</p><b>{x.submitterName}</b></div></div><div className="actions"><button className="approve" onClick={()=>action(x,"approved")}>审核通过并发布</button><button onClick={()=>action(x,"rejected")}>退回</button></div></article>)}</section>}
