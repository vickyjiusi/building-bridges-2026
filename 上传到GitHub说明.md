# 上传到 GitHub

目标仓库：

https://github.com/vickyjiusi/building-bridges-2026

## 操作步骤

1. 下载并解压本压缩包。
2. 打开目标 GitHub 仓库。
3. 点击 **Add file → Upload files**。
4. 打开解压后的 `building-bridges-2026-github` 文件夹。
5. 选择文件夹里面的全部文件和文件夹并拖入上传区域。
6. 等待文件上传完成。
7. 在 Commit changes 中填写：

```text
Add open-source migration starter
```

8. 点击 **Commit changes**。

注意：不要把外层文件夹本身再套一层上传。上传成功后，仓库首页应直接看到 `app`、`public`、`README.md` 和 `package.json`。
